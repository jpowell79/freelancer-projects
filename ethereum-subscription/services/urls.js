const API = '/api';
const USERS = `${API}/users`;
const SETTINGS = `${API}/settings`;
const SESSIONS = `${API}/sessions`;

module.exports = {
    base: API,
    users: USERS,
    settings: SETTINGS,
    sessions: SESSIONS
};